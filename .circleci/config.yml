version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    steps:
      - checkout
      - run: set | grep SECRET

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - build